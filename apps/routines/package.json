{
  "name": "@shedflare/routines",
  "version": "0.1.0",
  "private": true,
  "description": "A self-hosted daily routine tracker for Cloudflare.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shedflare/shedflare.git",
    "directory": "apps/routines"
  },
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "preview": "vp preview",
    "test": "vp test",
    "check": "vp check src build alchemy.run.ts vite.config.ts drizzle.config.ts",
    "typecheck": "vp check --no-fmt --no-lint",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "plan": "vp build && pnpm --dir ../.. exec alchemy plan apps/routines/alchemy.run.ts",
    "deploy": "pnpm --dir ../.. deploy:routines",
    "deploy:stage": "vp build && pnpm --dir ../.. exec alchemy deploy apps/routines/alchemy.run.ts --yes",
    "destroy": "pnpm --dir ../.. destroy:routines",
    "destroy:stage": "pnpm --dir ../.. exec alchemy destroy apps/routines/alchemy.run.ts --yes",
    "lint": "vp lint . --max-warnings=0"
  },
  "dependencies": {
    "@shedflare/alchemy": "workspace:*",
    "@shedflare/auth-client": "workspace:*",
    "@solidjs/meta": "^0.29.4",
    "@solidjs/router": "^0.15.4",
    "drizzle-orm": "catalog:",
    "peculiar-charts": "catalog:",
    "solid-js": "^1.9.12",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@playwright/test": "^1.60.0",
    "@shedflare/test-utils": "workspace:*",
    "@solidjs/testing-library": "^0.8.0",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "alchemy": "catalog:",
    "drizzle-kit": "catalog:",
    "effect": "catalog:",
    "jsdom": "catalog:",
    "jsonc-parser": "catalog:",
    "vite": "catalog:",
    "vite-plugin-solid": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "cloudflare": {
    "bindings": {
      "APP_PUBLIC_URL": {
        "description": "The canonical public URL of the app."
      },
      "OWNER_EMAIL": {
        "description": "Required owner email..."
      },
      "AUTH_ISSUER_URL": {
        "description": "Canonical public URL of the central Shedflare Auth issuer."
      },
      "AUTH_CLIENT_ID": {
        "description": "OAuth client ID used by this app when talking to Shedflare Auth."
      },
      "DEV_AUTH_EMAIL": {
        "description": "Optional local-only development identity used on localhost."
      }
    }
  }
}
