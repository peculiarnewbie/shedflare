/**
 * Compatibility re-export — settingsCollection is backed by a real reactive store.
 * All other collection stubs have been removed; routes fetch data directly.
 */

export { settingsCollection } from "./settings-store";
