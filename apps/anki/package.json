{
  "name": "@shedflare/anki",
  "version": "0.1.0",
  "private": true,
  "description": "A self-hosted, online-first spaced-repetition app for Cloudflare.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shedflare/shedflare.git",
    "directory": "apps/anki"
  },
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "preview": "vp preview",
    "test": "vp test",
    "test:live": "pnpm --dir ../.. test:anki",
    "check": "vp check src alchemy.run.ts alchemy.test.ts vite.config.ts build",
    "typecheck": "vp check --no-fmt --no-lint",
    "plan": "vp build && pnpm --dir ../.. exec alchemy plan apps/anki/alchemy.run.ts",
    "deploy": "pnpm --dir ../.. deploy:anki",
    "deploy:stage": "vp build && pnpm --dir ../.. exec alchemy deploy apps/anki/alchemy.run.ts --yes",
    "destroy": "pnpm --dir ../.. destroy:anki",
    "destroy:stage": "pnpm --dir ../.. exec alchemy destroy apps/anki/alchemy.run.ts --yes",
    "lint": "vp lint . --max-warnings=0"
  },
  "dependencies": {
    "@shedflare/auth-client": "workspace:*",
    "@solidjs/meta": "^0.29.4",
    "drizzle-orm": "catalog:",
    "solid-js": "^1.9.12",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@shedflare/alchemy": "workspace:*",
    "@shedflare/test-utils": "workspace:*",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "alchemy": "catalog:",
    "drizzle-kit": "catalog:",
    "effect": "catalog:",
    "jsonc-parser": "catalog:",
    "vite": "catalog:",
    "vite-plugin-solid": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "cloudflare": {
    "bindings": {
      "APP_PUBLIC_URL": {
        "description": "The canonical public URL of the app."
      },
      "OWNER_EMAIL": {
        "description": "Deployment owner email."
      },
      "AUTH_ISSUER_URL": {
        "description": "Canonical public URL of Shedflare Auth."
      },
      "AUTH_CLIENT_ID": {
        "description": "OAuth client ID used by this app."
      },
      "DEV_AUTH_EMAIL": {
        "description": "Optional local-only development identity used on localhost."
      }
    }
  }
}
